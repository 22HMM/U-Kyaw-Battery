<IfModule mod_rewrite.c>
    RewriteEngine On

    # Only rewrite if not an actual file or directory
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Rewrite everything to the public folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>